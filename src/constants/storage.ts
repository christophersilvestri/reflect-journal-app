export const STORAGE_KEYS = {
  JOURNAL_ENTRIES: 'reflect_journal_entries',
} as const;

export const DEFAULT_CONTENT_HEIGHT = 200;
export const LINE_HEIGHT = 24;
export const MIN_EDITOR_HEIGHT = 200; 